# Telegram Бот для Регистрации Клиентов

Профессиональный Telegram бот для быстрой регистрации клиентов на сайте specialist82.pro через API 1С.

## Быстрый старт (Development)

### 1. Установка
```bash
npm install
```

### 2. Настройка
```bash
# Создайте .env файл из примера
cp .env.example .env

# Отредактируйте .env - вставьте свои данные
nano .env
```

### 3. Конвертация базы клиентов
```bash
# Убедитесь что файл Клиенты.xlsx в корне проекта
npm run convert
```

### 4. Запуск бота
```bash
npm start
```

### 5. Использование
1. Откройте бота в Telegram
2. Отправьте `/start`
3. Введите пароль (из вашего .env)
4. Готово!

## 🚀 Production деплой

Смотрите [DEPLOY.md](DEPLOY.md) для подробных инструкций по деплою на сервер.

## Основной функционал

- **Авторизация** - единожды по паролю, сохраняется навсегда
- **Поиск клиентов** - нечеткий поиск (работает даже с опечатками) по базе 3621 клиентов
- **Inline-кнопки** - выбор из 5 самых подходящих вариантов
- **Валидация** - автопроверка телефона и email
- **API интеграция** - отправка POST запроса с кодом из 1С
- **База данных** - SQLite для пользователей, сессий и истории
- **Навигация** - кнопки "Назад" и "Отменить" на каждом этапе
- **Статистика** - личная и общая статистика регистраций
- **Уведомления** - автоматическая отправка уведомлений о новых регистрациях в группу
- **Модерация** - подтверждение/отказ в регистрации через inline-кнопки в группе
- **Создание ЛК** - автоматическое создание личного кабинета при подтверждении

## Структура проекта

```
bot_specialist_site/
├── src/
│   ├── index.js              # Главный файл бота
│   ├── config/               # Конфигурация
│   ├── database/             # Работа с БД (SQLite)
│   ├── handlers/             # Обработчики (авторизация, регистрация)
│   ├── keyboards/            # Клавиатуры бота
│   ├── services/             # Бизнес-логика (API, поиск клиентов)
│   ├── middlewares/          # Обработка ошибок
│   └── utils/                # Утилиты и логирование
├── scripts/
│   ├── convertExcel.js       # Конвертация Excel → JSON
│   ├── stats.js              # Просмотр статистики
│   ├── testApi.js            # Тестирование API
│   ├── cleanDatabase.js      # Очистка БД
│   └── checkSetup.js         # Проверка установки
├── data/
│   ├── clients.json          # База клиентов (3621)
│   └── users.db              # БД пользователей
├── logs/
│   └── bot.log               # Логи работы
├── .env                      # Конфигурация
├── package.json
└── README.md
```

## Конфигурация (.env)

**Важно!** Создайте `.env` файл из `.env.example` и заполните своими данными:

```bash
cp .env.example .env
nano .env
```

Пример содержимого `.env`:
```env
# Telegram
TELEGRAM_BOT_TOKEN=your_bot_token_here
BOT_PASSWORD=your_password_here

# Admin (ID администратора для регистрации без подтверждения)
ADMIN_ID=your_admin_telegram_id_here

# Notifications (ID группы для уведомлений о регистрациях)
NOTIFICATION_GROUP_ID=-1003421862306

# Create LK API (для подтверждения регистраций)
CREATE_LK_API_URL=https://specialist82.pro/create_lk_api.php
CREATE_LK_API_TOKEN=your_secret_token_here

# API
API_URL=https://your-api-url.com/endpoint
API_ACCESS_TOKEN=your_api_token_here

# Настройки
MAX_SEARCH_RESULTS=5
SESSION_TIMEOUT=1800000
LOG_LEVEL=info
```

**⚠️ Никогда не коммитьте .env файл с реальными данными!**

## Команды

```bash
npm start         # Запуск бота
npm run dev       # Режим разработки (автоперезагрузка)
npm run convert   # Конвертация Excel в JSON
npm run stats     # Статистика
npm run test-api  # Тест API
npm run check     # Проверка установки
npm run clean-db  # Очистка базы данных
```

## Использование бота

### Первый запуск
1. `/start` - Запустить бота
2. Ввести пароль: `22170313`
3. Авторизация сохраняется навсегда

### Регистрация клиента (3 шага)
1. **Поиск** - нажмите "🔍 Найти клиента", введите название
2. **Телефон** - введите телефон (например: `+79787599070`)
3. **Email** - введите email (например: `user@example.com`)
4. Подтвердите и готово!

### Команды бота
- `/start` - Главное меню
- `/help` - Справка
- `/stats` - Статистика
- `/cancel` - Отменить операцию

### Модерация в группе

При каждой новой регистрации в группу приходит уведомление с кнопками:

- **✅ Подтвердить** - создаёт ЛК через API и уведомляет пользователя
- **❌ Отказать** - отклоняет регистрацию и уведомляет пользователя

После нажатия кнопки:
- Статус в сообщении обновляется
- Кнопки скрываются
- Пользователь получает уведомление о результате

### Регистрация без подтверждения (для админа)

Если вы администратор (ID указан в `ADMIN_ID` в `.env`), в меню доступна кнопка:

- **⚡ Рег. без подтверждения** - регистрация с автоматическим созданием ЛК

При использовании этой опции:
1. Клиент регистрируется на сайте
2. ЛК создаётся автоматически
3. Уведомление в группу отправляется БЕЗ кнопок модерации
4. Статус сразу "ПОДТВЕРЖДЕНО И СОЗДАНО"

## API Integration

### 1. Регистрация клиента

POST запрос к `shop.customer.add`:

```bash
POST https://specialist82.pro/api.php/shop.customer.add?access_token=TOKEN

Headers:
  Content-Type: application/x-www-form-urlencoded

Body:
  data[name]=Имя клиента
  data[email][0][value]=email@example.com
  data[email][0][ext]=work
  data[phone][0][value]=+79787599070
  data[phone][0][ext]=mobile
  data[kodv1s]=код_из_1с (из поля "Код" в Excel)
```

### 2. Создание ЛК (после подтверждения)

POST запрос к `create_lk_api.php`:

```bash
POST https://specialist82.pro/create_lk_api.php

Headers:
  Content-Type: application/x-www-form-urlencoded

Body:
  token=SUPER_SECRET_TOKEN_123
  contact_id=ID созданного контакта из первого запроса
```

Этот запрос отправляется автоматически при нажатии кнопки "Подтвердить" в группе.

## База данных

SQLite база с 3 таблицами:

### users
Авторизованные пользователи
- chat_id, username, first_name, last_name
- authorized_at, last_activity

### registration_sessions
Текущие сессии регистрации
- chat_id, client_name, client_code, phone, email, step

### registration_history
История всех регистраций
- chat_id, client_name, client_code, phone, email, status, api_response

## Технологии

- **Node.js** - среда выполнения
- **node-telegram-bot-api** - интеграция с Telegram
- **SQLite3** - база данных
- **Fuse.js** - нечеткий поиск
- **Axios** - HTTP клиент (с retry механизмом)
- **Winston** - логирование
- **XLSX** - работа с Excel
- **dotenv** - переменные окружения

## Улучшения

- ✅ Fuzzy Search - находит клиентов даже при опечатках
- ✅ Retry механизм - 3 автоматических повторных попытки при ошибках API
- ✅ Валидация и автоформатирование данных
- ✅ Система сессий с восстановлением после перезапуска
- ✅ История всех регистраций в БД
- ✅ Профессиональное логирование (Winston)
- ✅ Graceful shutdown
- ✅ Обработка всех типов ошибок
- ✅ Подтверждение данных перед отправкой
- ✅ Кнопки "Назад" и "Отменить"
- ✅ Emoji в интерфейсе
- ✅ Отслеживание активности пользователей
- ✅ Модульная архитектура
- ✅ Уведомления в группу при каждой успешной регистрации
- ✅ Модерация регистраций через inline-кнопки в группе
- ✅ Автоматическое создание ЛК при подтверждении
- ✅ Уведомления пользователей о результате модерации

## Отладка

### Просмотр логов
```bash
tail -f logs/bot.log
```

### Просмотр БД
```bash
sqlite3 data/users.db
SELECT * FROM users;
SELECT * FROM registration_history LIMIT 10;
```

### Статистика
```bash
npm run stats
```

### Тест API
```bash
npm run test-api
```

## Troubleshooting

### Бот не отвечает
1. Проверьте что бот запущен: `npm start`
2. Проверьте токен в `.env`
3. Проверьте логи: `tail -f logs/bot.log`

### Клиенты не найдены
```bash
npm run convert  # Пересоздать базу клиентов
```

### Ошибка API
1. Проверьте `API_URL` и `API_ACCESS_TOKEN` в `.env`
2. Протестируйте: `npm run test-api`
3. Проверьте доступность сервера specialist82.pro

### Ошибка SSL/TLS
Бот автоматически повторит запрос 3 раза. Если не помогает:
- Проверьте интернет-соединение
- Проверьте доступность API сервера
- Возможны технические работы на сервере

### Уведомления не приходят в группу
1. Проверьте что бот добавлен в группу
2. Проверьте что у бота есть права на отправку сообщений
3. Проверьте правильность ID группы в `.env`
4. Для получения ID группы: добавьте бота в группу и отправьте команду `/start`

## Лицензия

ISC

---

**Версия:** 1.0.0  
**Дата:** 28 ноября 2025  
**Сделано для:** specialist82.pro
